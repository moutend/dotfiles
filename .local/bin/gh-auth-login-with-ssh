#!/usr/bin/env bash

すでに`gh auth login`済みで、既存の公開鍵だけGitHubに追加したい場合は次です。
# gh ssh-key add ~/.ssh/id_ed25519.pub --title "$(hostname)-$(date +%Y-%m-%d)"

gh auth login --hostname github.com --git-protocol ssh
